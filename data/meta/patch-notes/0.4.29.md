# ForgeMirror 0.4.29

- Profile UX: блок `Текущие задачи` в профиле получил summary по активным, просрочке, pending XP и ближайшему фокусу вместо одной строковой статистики.
- Profile UX: добавлены быстрые переходы из профиля в `Задачи` по режимам `Все`, `Активные`, `Просрочка`, `Ждут XP`.
- Profile UX: в таблице активных задач профиль теперь показывает категорию, penalty-маркер и более компактный pipeline `code + title`, а в секции выполненных задач появился быстрый переход к pending XP.
