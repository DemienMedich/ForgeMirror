# ForgeMirror 0.4.31

- Capture UX: для Win32/OpenGL fullscreen добавлена синхронизация `glFinish -> glfwSwapBuffers -> DwmFlush`, чтобы системный захват окна реже получал устаревший кадр.
- Build: GUI теперь явно линкуется с `dwmapi`, потому что present-path использует `DwmFlush`.
- Capture: это отдельный шаг поверх windowed fullscreen из `0.4.30`, нацеленный именно на stale-frame у `Alt+PrtScn`.
