# ForgeMirror 0.4.33

- Cloud stability: fixed repeated overwrite of `meta/cloud.ini` and cloud `manifest.ini` on Windows.
- QA: added smoke coverage for repeated cloud config/manifest saves so this regression is caught automatically.
- Repo hygiene: `build-test/` is now ignored as a generated smoke-build directory.
