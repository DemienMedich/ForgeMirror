# ForgeMirror 0.4.05

- Архитектура: профильные CRUD-операции и мутации ачивок в `GuiActions.cpp` переведены на `AppProfileMutationService`.
- GUI: каталог навыков передаёт `profileId` в сервисный слой ачивок, без прямого сохранения профиля из UI.
- Подготовка к замене GUI: старый профильный mutation-центр в `GuiActions` сведён к совместимым thin-wrapper вызовам.