# Patch Notes 0.3.84

- `Логи`: добавлен блок `Аудит задач` с последними изменениями из `meta/task-audit.log`.
- `Логи`: добавлен экспорт журнала задач в `meta/reports/task-audit-YYYYMMDD-HHMMSS.txt`.
- `Логи`: журнал задач теперь доступен в едином окне логов, без перехода в модуль задач.
