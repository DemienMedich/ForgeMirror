# Patch Notes 0.3.83

- `Задачи`: добавлен аудит изменений в `meta/task-audit.log`.
- `Задачи`: логируются создание, удаление, смена статуса/приоритета/проекта/дедлайна/исполнителей, включая массовые операции.
- `Задачи`: в UI добавлен блок `Последние изменения` с последними записями журнала.
- `Облако`: `meta/task-audit.log` добавлен в синхронизацию (pull/push) и в очистку лишних файлов по whitelist.
