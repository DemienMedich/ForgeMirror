# ForgeMirror 0.4.34

- QA: added smoke coverage for workspace drift, resolve and backup-restore flows for `tasks.json` and `pipeline.json`.
- Profile UX: rebuilt the profile summary center with operational signal cards and quick actions, so the start screen carries more useful context at a glance.
