# ForgeMirror 0.4.01

- Архитектура: добавлен `AppPipelineService` для операций пайплайна вне GUI-слоя.
- Пайплайн: сохранение, добавление, удаление и перестановка шагов переведены из `GuiPipelinePanel.inc` в app-service.
- GUI: удалён устаревший `gui/GuiPipeline.inc` с дублирующей JSON-логикой.
- Надёжность: операции пайплайна теперь идут через единый rollback-сценарий при ошибке сохранения.
- Сборка: `ForgeMirrorGui`, `ForgeMirror`, `smoke_core` проходят на версии `0.4.01`.
