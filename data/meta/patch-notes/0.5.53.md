# ForgeMirror 0.5.53

- Правила workflow задач перенесены в `AppTaskWorkflowService`: переходы статусов, распределение XP, штраф за дедлайн и валидация финализации XP.
- Старый публичный API задач сохранён как совместимый фасад, который делегирует workflow-сервису.
- Smoke-тест закрепляет границу: критичная XP-валидация и расчёт штрафа должны иметь реальную реализацию в workflow-сервисе.
