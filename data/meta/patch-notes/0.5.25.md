# ForgeMirror 0.5.25

- Исправлен ImGui stack warning в основном списке задач после перехода на compact table scope.
- `CompactTableScopeGui` в таблице задач теперь живёт строго внутри области таблицы и child-window, без позднего `PopStyleVar`.
