# ForgeMirror 0.3.97

- Продолжена подготовка к будущей смене GUI: профильные операции вынесены в отдельный service-слой.
- Добавлены `include/AppProfileService.h` и `src/AppProfileService.cpp`.
- GUI больше не сортирует/перезагружает активный профиль напрямую: `GuiProfileOps.inc` использует общий app-service.
- Из GUI-хелперов убран локальный `LoadProfiles`, профильный reload теперь живёт вне GUI-слоя.
