# ForgeMirror 0.4.48

- Navigation: disabled the 3D viewer module by default and hid its menu, navigation and workspace entry points.
- Tasks: merged the task mode switch into the main toolbar and added always-visible task search there.
- Tasks: removed the separate task-mode card to make the top of the task screen more compact.
