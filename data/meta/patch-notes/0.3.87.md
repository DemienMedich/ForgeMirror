# Patch Notes 0.3.87

- `Задачи`: в блок `Массовые действия` добавлено массовое изменение `приоритета` для выбранных задач.
- `Задачи`: добавлен отдельный контрол выбора приоритета (`Низкий/Средний/Высокий/Критический`) и кнопка применения.
- `Задачи`: массовая смена приоритета пишет аудит в `meta/task-audit.log` и откатывает изменения при ошибке сохранения.

