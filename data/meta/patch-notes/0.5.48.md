# ForgeMirror 0.5.48

- Добавлены локальные last-good копии задач и пайплайна.
- Повреждённые `tasks.json` и `pipeline.json` автоматически восстанавливаются при запуске.
- Сохранение JSON переведено на общий атомарный файловый слой с корректной заменой файлов в Windows.
- XP-модалка больше не обходит сервисное сохранение задач.
