# ForgeMirror 0.4.49

- Tasks: removed the duplicate search field from the expanded filters block after moving search into the main toolbar.
- Tasks: compacted task export actions into icon buttons with tooltips.
- Tasks: deduplicated CSV/TXT export handling inside the tasks panel.
