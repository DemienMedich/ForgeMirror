# Patch Notes 0.3.86\n\n- `Задачи`: добавлен фильтр по проекту в основном блоке фильтров (для узкого и широкого layout).\n- `Задачи`: в фильтрах доступны режимы `Все проекты` / `Без проекта` / конкретный проект.\n- `Задачи`: фильтр сбрасывается кнопкой `Сбросить фильтр` вместе с остальными параметрами.\n