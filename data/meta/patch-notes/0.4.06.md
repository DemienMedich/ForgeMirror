# ForgeMirror 0.4.06

- Pipeline: в админском режиме у каждого блока появилась кнопка `Ред.`, а detail-pane переключается в inline-редактор полного содержимого блока.
- Pipeline: сервис обновления шага теперь сохраняет весь блок целиком, а не только `title/description`.
- UX review: подготовлены рекомендации по улучшению модуля `Задачи` на основе текущей структуры экрана.
