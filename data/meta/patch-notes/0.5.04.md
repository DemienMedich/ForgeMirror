# ForgeMirror 0.5.04

- Пайплайн: исправлен ImGui stack warning `Missing PopStyleVar()` при открытии вкладки.
- Пайплайн: внешний `pipeline_split` теперь закрывает `PopStyleVar()` после завершения таблицы, а не внутри detail-pane.
- Пайплайн: строка фильтра приведена к одному стабильному compact-table блоку.
- QA: smoke проверяет базовый layout-контракт пайплайна для filter/actions/split style stack.
