# Patch Notes 0.3.74

- Storage: `storage.json` теперь всегда записывается в locale-independent формате (без разделителей тысяч).
- Cloud Sync: чтение `updated_at`/`rev` устойчиво к посторонним символам, чтобы не ломать выбор актуальной версии.
