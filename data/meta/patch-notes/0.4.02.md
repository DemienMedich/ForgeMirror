# ForgeMirror 0.4.02

- Архитектура: добавлен `AppMetaService` для операций `banner` и `vault` вне GUI-слоя.
- Баннер: добавление, редактирование и удаление фраз переведены из `GuiBannerPanel.inc` в app-service.
- Хранилище: сохранение draft-настроек (`currency`, `logLimit`, правила помодоро) переведено из `GuiVaultPanel.inc` в app-service.
- Консистентность: после сохранения `vault` состояние перечитывается из `storage.json`, чтобы GUI работал с нормализованными актуальными данными.
- Сборка: `ForgeMirrorGui`, `ForgeMirror`, `smoke_core` проходят на версии `0.4.02`.
