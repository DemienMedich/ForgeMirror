# Patch Notes 0.3.75

- QA: добавлен smoke-тест для `storage.json` с поврежденными разделителями в числах (`updated_at`, `pomodoro_end`).
- QA: проверяется, что после сохранения `storage.json` числа нормализуются и пишутся без невалидных символов.
