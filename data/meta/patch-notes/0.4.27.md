# ForgeMirror 0.4.27

- Tasks UX: в таблице задач lifecycle-статус очищен от вторичных сигналов; XP handoff теперь показывается отдельной меткой под названием задачи.
- Tasks UX: колонка `Этап` для пользователей читает pipeline как короткий `code + title`, сохраняя progress-hint и подсветку рисков.
- Tasks UX: колонка `Исполнители` схлопывает длинные списки до компактного summary и раскрывает полный состав по tooltip.
