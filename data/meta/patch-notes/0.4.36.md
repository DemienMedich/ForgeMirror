# ForgeMirror 0.4.36

- Tasks UX: added an `Оперативно / Администрирование` switch so everyday task work is separated from admin tooling.
- Tasks UX: task creation, project bridge, audit preview, bulk actions and inline table edits now stay in admin mode; work mode keeps filters, deadlines, table and task details focused.
