# ForgeMirror 0.4.53

- QA: added a smoke guard for task-panel ImGui card stack patterns.
- QA: smoke now fails if `GuiTasksPanel.inc` reintroduces `&& BeginCard(...)` or unbalanced `BeginCard/EndCard`.
