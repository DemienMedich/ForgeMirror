# ForgeMirror 0.4.25

- Cloud UX: confirm-modal для `Принять облачную`, `Оставить локальную` и `Восстановить` теперь показывает quick-diff `до -> после` по ключевым метрикам и размеру файла.
- Cloud UX: quick-diff умеет сравнивать не только live `local/cloud` версии, но и backup-снимки из `meta/updates` для `tasks.json` и `pipeline.json`.
- Cloud UX: preview backup-файлов переведён на тот же доменный разбор, что и live-preview, а мусорный duplicate-include в `GuiMainMenuPanel.inc` убран.
