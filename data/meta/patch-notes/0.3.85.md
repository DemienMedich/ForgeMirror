# Patch Notes 0.3.85\n\n- `Логи`: в блок `Аудит задач` добавлены фильтры по актору, задаче/ID и полю.\n- `Логи`: добавлена кнопка `Открыть файл аудита` (meta/task-audit.log).\n- `Логи`: исправлены логические проверки (`||` вместо `|`) в подсчёте/фильтрах.\n