# Patch Notes 0.3.79

- `Задачи`: добавлена защита переходов статуса — запрещён прямой переход `Выполнена -> Новая`.
- `Задачи`: массовые действия теперь применяются атомарно по типу операции (одно сохранение на операцию, с откатом при ошибке).
- `Задачи`: массовые операции показывают расширенный результат (обновлено/пропущено).
- `Задачи`: добавлен индикатор активного быстрого фильтра.
- `Задачи`: исправлен расчёт активности за 14 дней (`||` вместо побитового `|` в проверке диапазона).
