# ForgeMirror 0.4.24

- Cloud UX: действия `Принять облачную`, `Оставить локальную` и `Восстановить` больше не выполняются мгновенно; перед ними открывается confirm-modal.
- Cloud UX: confirm-modal показывает preview источника и цели по `tasks.json` и `pipeline.json`: время изменения, размер файла и краткую доменную сводку.
- Cloud UX: для `tasks.json` preview показывает число задач и pending XP, для `pipeline.json` — число этапов, ветвлений и финальных шагов.
