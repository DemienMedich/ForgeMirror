# ForgeMirror 0.4.44

- Spirits: profile badges now use PNG icons from `data/spirits` with fallback to built-in glyphs.
- Storage: bundled spirit icons are copied into user storage on startup like seeded achievement icons.
- Cloud: `spirits/*.png` is now included in sync push/pull and whitelist cleanup.
