Goal (incl. success criteria):
- Зафиксировать текущую версию как стабильную (после отката до INI).
- Привести облачную синхронизацию к рабочему варианту согласно согласованным требованиям.
- Подготовить удобный запуск на других устройствах (Windows, установщик) и продумать обновление программы.
- Сформировать план улучшения стабильности и UX, включая корректировки кнопок и кнопку "Обзор" для выбора папки синхронизации.
- Расширить аналитику в "Задачах" (кликабельные строки, детали опыта/навыков), исправить тултип описания.
- Дать обычным пользователям доступ к настройкам интерфейса (только "во весь экран" и "без рамки").
- Ограничить настройки звуков Помодоро только администратору.
- Запланировать окно "Ярлыки" с правой панелью иконок запуска программ.
- Перенести CONTINUITY.md в AgentsSkills и учитывать переименование SkillAgents -> AgentsSkills.
- Полный редизайн UI в техно-стекло (без градиентов) с приоритетом: главное меню -> профиль -> рабочие панели; главное меню - фиксированная верхняя плашка; профили в отдельной вкладке; выбор профиля доступен в окне профиля.
- Дальше улучшать UI по референсу: аккуратные иконки, графики/диаграммы (без градиентов), не ломая текущие сценарии.
- Добавить систему заданий: при начислении опыта фиксировать проект/задачу/описание/время; поддержать несколько профилей с распределением вклада; показать задачи в профилях и в отдельном меню.
- Добавить таймер Помодоро: отдельное окно с настройками, отсчёт в боковом меню, звуковой сигнал по окончанию интервалов, подтверждение старта следующего интервала.
- Сборки Release (CLI + GUI) проходят после правок UI.
- UX: запоминать последний профиль, окно "Профили" по умолчанию закрыто, диаграмма уровня центрирована.
- Layout: все панели/окна справа как вкладки (единая область), навигация слева.
- Версия APP_VERSION инкрементируется при заметных изменениях UI/UX.

Constraints/Assumptions:
- Ответы на русском; обновлять CONTINUITY.md в начале каждого ответа и при смене цели/решений/состояния.
- Ответы кратко.
- Без градиентов; плашка окна равна фону окна (даже при фоне/текстуре).
- После правок UI запускать `cmake --build build --config Release` и `cmake --build build-gui --config Release --target JobSkillGui`.
- Вести контроль версий; не коммитить CONTINUITY.md; без разрушительных git-команд.
- kDecayEnabled=true (включён).
- Локаль classic/UTF-8 для стабильных чисел/строк.

Key decisions:
- Папка SkillAgents переименована в AgentsSkills; использовать AgentsSkills.
- CONTINUITY.md и AGENTS.md храним в AgentsSkills.
- Визуальный язык: техно-стекло, тёмная база, закруглённые карточки, лёгкое свечение, без градиентов.
- Приоритет редизайна: главное меню -> профиль -> рабочие панели.
- Верх экрана трактуется как верх профиля (не глобальная плашка).
- Двигаемся строго по этапам плана, без UI-героизма; старт с профиля.
- Плашка окна не отделяется от фона.
- Разрешены изменения компоновки ради удобства.
- Тёмная базовая палитра подтверждена.
- Главное меню: фиксированная верхняя плашка (ImGui main menu bar) вместо отдельного окна.
- Профили: отдельная вкладка в рабочей области + быстрый выбор в окне профиля.
- Иконки: встроенные векторные (ImDrawList) + компактные icon-button'ы (без внешних шрифтов/паков).
- Диаграммы: добавить кольцевые/прогресс-диаграммы (без градиентов) для ключевых метрик профиля.
- Последний профиль: сохранять ID в `meta/ui.json` и восстанавливать при запуске (если найден).
- Окно "Профили": по умолчанию закрыто.
- Layout: отказаться от отдельных окон (Профиль/Профили/Рабочее окно) в пользу единого окна справа с TabBar.
- Версионирование: APP_VERSION хранится в `CMakeLists.txt` и увеличивается при UI-изменениях.
- Напоминание: при значимых изменениях программы инкрементировать APP_VERSION в `CMakeLists.txt`.
- Этап 2.1 начинаем с таблицы профиля "Навыки".
- Система заданий хранится в отдельном файле; задачи видны в профилях участников.
- Расширяем текущий модал "Добавить опыт" полями задачи + выбором профилей.
- В профиле показываем последние 5 задач во вкладке; полный список доступен в отдельном меню.
- Хранение задач без ограничений (зафиксировать как риск на будущее).
- Задачи добавляет только администратор.
- Распределение вклада по профилям суммируется в 100%.
- Хранение задач: `data/meta/tasks.json`.

State:
- Выполнен откат репозитория к состоянию перед миграцией INI->JSON (`git restore --source=15be8b5 -- .`).
- Последние UI-правки: disabled-стили для пустых состояний/плейсхолдеров, hint в правилах, логи с пустым состоянием, общая техно-стекло-стилизация и единая верхняя плашка.
- Сборки Release (CLI + GUI) после отката успешны.
- Удалены все JSON-файлы в пользовательских хранилищах (`%APPDATA%\\ForgeMirror`, `%APPDATA%\\JobSkill`) и папки `meta/ui-presets`.
- После отката проблема с слётом alpha/темы не воспроизводится.
- Текущая версия считается стабильной до новых указаний.
- Облачная синхронизация: добавлена индикация активного пути в настройках и в меню/tooltip кнопки; кнопка синка недоступна при отключённом облаке.
- Release-сборки (CLI + GUI) успешны после правок облака.
- Новая проблема: в папке синхронизации не появляются данные; пользователь в режиме просмотра (админ-разделы скрыты) — нужно проверить выгрузку админом/настройки авто-пуша.
- Проверено локально: `%APPDATA%\ForgeMirror\meta\cloud.ini` содержит enabled=1, autoPull=1, autoPush=1, root=Z:\Yandex.Disk\ForgeMirrorSync.
- Новый запрос: подготовить программу для запуска на других компьютерах и продумать механизм обновления.
- Уточнено: целевая ОС - Windows; предпочтителен установщик.
- Добавлены: Inno Setup скрипт `installer/ForgeMirror.iss` и сборочный скрипт `installer/build-installer.ps1`.
- Установщик обновлён: только GUI (CLI исключён).
- UI: добавлена возможность запускать установщик после загрузки обновления из облака.
- Сборки Release (CLI + GUI) успешны после правок установщика и меню обновлений.
- Новый фидбек: на втором устройстве в "нашей версии" нет профилей (кроме админа), в установленной версии профили есть; в установленной версии применён нестандартный пресет темы; нужен значок для установленного exe.
- Профили: добавлено слияние seed-профилей из `data/` в пользовательское хранилище (без перезаписи).
- UI: дефолтная тема техно-стекло применяется при отсутствии `meta/ui.ini`; копирование `ui.ini` и `gui-layout.ini` из seed отключено.
- Сборки Release (CLI + GUI) успешны после правок профилей/темы.
- Добавлена минимальная иконка FM (`installer/ForgeMirror.ico`), ресурс `gui/ForgeMirrorGui.rc`, установщик использует `SetupIconFile`.
- Сборки Release (CLI + GUI) успешны после добавления иконки.
- Установщик: AppVerName и VersionInfoVersion/TextVersion берутся из `AppVersion`.
- В верхнем баре добавлен индикатор синхронизации (состояние + tooltip), рядом с кнопкой синка.
- Сборки Release (CLI + GUI) успешны после добавления индикатора синхронизации.
- Убрано автосоздание профиля Admin в GUI/CLI; админ-режим в CLI больше не привязан к флагу профиля.
- Сборки Release (CLI + GUI) успешны после отключения автосоздания Admin.
- Удалены профили `0001.ini` и `0006.ini` из `%APPDATA%\\ForgeMirror` и `data/0006.ini`.
- Облако: при выгрузке админом удаляются лишние профили (.ini) в папке синхронизации и в archive.
- Сборки Release (CLI + GUI) успешны после добавления очистки облака.
- Облако: при выгрузке админом удаляется `meta/tasks.json`, если локально его нет.
- Сборки Release (CLI + GUI) успешны после удаления orphan tasks.json в облаке.
- Просмотрена папка AgentsSkills (AGENTS.md, CONTINUITY.md, DesignSkill.md).
- Добавлен индикатор состояния сохранения в настройках UI; сборки Release (CLI + GUI) успешны.
- Сделано: главное меню перенесено в фиксированную верхнюю плашку; профили вынесены в отдельную вкладку; выбор профиля добавлен в окно профиля.
- Тестовые сборки Release (CLI + GUI) после правок успешны; изменения закоммичены (без CONTINUITY.md).
- Новый запрос: продолжать улучшать UI по референсу (иконки, графики, диаграммы).
- Добавлены: векторные иконки/икон-кнопки в верхней плашке; кольцевая диаграмма прогресса/категорий в профиле; KPI-карточки с иконками. Release-сборки (CLI+GUI) успешны после правок; изменения закоммичены (без CONTINUITY.md).
- Добавлено: иконки в навигации (компактные nav-кнопки); новые мини-диаграммы (sparklines/mini-bars) в табе профиля "Диаграммы"; Release-сборки (CLI+GUI) успешны после правок; изменения закоммичены (без CONTINUITY.md).
- Сделано: диаграмма уровня центрирована; окно "Профили" по умолчанию закрыто; последний профиль сохраняется/восстанавливается через `meta/ui.ini`. Release-сборки (CLI+GUI) успешны после правок; изменения закоммичены (без CONTINUITY.md).
- Сделано: убран warning Dear ImGui (SetCursorPos) в заголовке профиля; в каталоге навыков добавлена карточка "Сводка" с mini-bars по категориям веса. Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: улучшена "Статистика" (sparklines/mini-bars для категорий и рангов); окно "Профиль" зафиксировано в рабочей области; таблица "Состояние" стабилизирована (нет дрожания от таймера). Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: APP_VERSION обновлён до 0.2.6; все панели справа объединены в единый TabBar (профиль + прочие вкладки). Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: панель "Логи" получила сводку с KPI/иконками и спарклайном активности; APP_VERSION обновлён до 0.2.7. Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: панель "Каталог навыков" получила сводку с KPI/иконками, спарклайном веса и расширенной статистикой; APP_VERSION обновлён до 0.2.8. Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: панель "Админ-статистика" получила KPI-карточки с иконками; APP_VERSION обновлён до 0.2.9. Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: в профиле добавлена кнопка "Добавить опыт" (открывает модал XP); APP_VERSION обновлён до 0.2.10. Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Сделано: кнопка "Добавить опыт" перенесена под имя/ID в профиле; программа переименована в ForgeMirror (UI/доки/окно/выходные файлы); APP_VERSION обновлён до 0.2.11. Release-сборки (CLI+GUI) успешны; изменения закоммичены (без CONTINUITY.md).
- Новый запрос: следовать плану развития UI/UX (этапы 1-5) с фокусом на направляющий интерфейс.
- Этап 1.2 (профиль): верхняя зона уплотнена - добавлена краткая сводка (уровень/ранг/прогресс), убраны дубли под диаграммой; APP_VERSION обновлён до 0.2.12; изменения закоммичены.
- Этап 1.3 (профиль): добавлены признаки загрузки и ошибки при выборе профиля; UI показывает состояние загрузки, ошибку и пустые списки; изменения закоммичены; APP_VERSION=0.2.13.
- Этап 1.1 (каталог навыков): добавлены подсказки действий и пустой стейт, CTA для добавления при пустом выборе; APP_VERSION=0.2.14; Release-сборки успешны.
- Этап 1.1 (пайплайн): добавлены подсказки действий и понятный empty-state для фильтра; APP_VERSION=0.2.15; Release-сборки успешны.
- Этап 1.1 (логи): добавлены подсказки действий, empty-state и быстрый сброс фильтров; APP_VERSION=0.2.16; Release-сборки успешны.
- Этап 1.1 (статистика): добавлены подсказки назначения и сценариев при пустых данных/фильтрах; APP_VERSION=0.2.17; Release-сборки успешны.
- Этап 1.1 (правила): добавлены подсказки назначения и контекста изменений; APP_VERSION=0.2.18; Release-сборки успешны.
- Этап 1.1 (3D просмотр): добавлены подсказки управления и контекст ошибки; APP_VERSION=0.2.19; Release-сборки успешны.
- Этап 1.1 (3D настройки): добавлены подсказки назначения и empty-state моделей; APP_VERSION=0.2.20; Release-сборки успешны.
- Этап 1.1 (настройки интерфейса): добавлены подсказки назначения и empty-state фонов; APP_VERSION=0.2.21; Release-сборки успешны.
- Этап 1.1 (главное меню): добавлены подсказки назначения, быстрые действия и empty-state профилей; APP_VERSION=0.2.22; Release-сборки успешны.
- Этап 1.1 (навигация): добавлены подсказка назначения и статус видимости админ-разделов; APP_VERSION=0.2.23; Release-сборки успешны.
- UI: все сворачиваемые секции теперь по умолчанию свернуты; APP_VERSION=0.2.24; Release-сборки успешны.
- Идея на будущее: добавить функционал task-менеджера.
- Этап 2.1 (логи): ключевой столбец выделен, добавлены микро-якоря и акцент для важных строк; APP_VERSION=0.2.26; Release-сборки успешны.
- Этап 2.1 (профиль/навыки): ключевой столбец выделен, добавлены микро-якоря и ритм топ-строк; APP_VERSION=0.2.25; Release-сборки успешны.
- Этап 2.1 (каталог навыков): ключевой столбец выделен, добавлены микро-якоря и ритм топ-строк; APP_VERSION=0.2.27; Release-сборки успешны.
- Этап 2.1 (админ-статистика): ключевой столбец выделен, добавлены микро-якоря и ритм топ-строк; APP_VERSION=0.2.28; Release-сборки успешны.
- Этап 2.1 (профиль/топ навыков): ключевой столбец выделен, добавлены микро-якоря и ритм топ-строк; APP_VERSION=0.2.29; Release-сборки успешны.
- Этап 2.1 (пайплайн): список усилен (ключевой столбец, микро-якоря); APP_VERSION=0.2.30; Release-сборки успешны.
- Этап 2.1 (профиль/состояние): ключевой столбец выделен, добавлены микро-якоря и акцент проблемных строк; APP_VERSION=0.2.31; Release-сборки успешны.
- Этап 2.1 (правила): ключевой столбец выделен, добавлены микро-якоря и единицы; APP_VERSION=0.2.32; Release-сборки успешны.
- Этап 2.1 (3D/настройки UI): табличные фильтры усилены (микро-якоря, ключевой столбец); APP_VERSION=0.2.33; Release-сборки успешны.
- Этап 2.1 (профиль/индикаторы): ключевой столбец выделен, добавлены микро-якоря и акцент проблемных строк; APP_VERSION=0.2.34; Release-сборки успешны.
- Этап 2.1 (модальные окна): табличные блоки действий усилены (микро-якоря, ключевой столбец); APP_VERSION=0.2.35; Release-сборки успешны.
- Этап 2.1 (прочие фильтры/экшены): добавлены микро-якоря и ключевой столбец в фильтрах/экшенах; APP_VERSION=0.2.36; Release-сборки успешны.
- Обратная связь: появились числовые якоря в таблицах; в "Пользовательских пресетах" кнопки визуально сжались.
- Исправлено: кнопки в "Пользовательских пресетах" восстановлены (заданы ширины колонок); APP_VERSION=0.2.37; Release-сборки успешны.
- Новый запрос: заменить числовые микро-якоря на иконки и продолжать задачи по списку.
- Сделано: якоря заменены на иконки; XP-модал усилен (якоря, ключевой столбец, ритм топ-строк); APP_VERSION=0.2.38; Release-сборки (CLI + GUI) успешны.
- Подтверждение: иконки устраивают, двигаться дальше по этапам.
- Сделано: фильтры профиля (ачивки/навыки/активность) усилены якорями и ключевыми колонками; APP_VERSION=0.2.39; Release-сборки успешны.
- Сделано: фильтры логов усилены якорем и ключевой колонкой; APP_VERSION=0.2.40; Release-сборки успешны.
- Сделано: админ-статистика (строки обновления/фильтров) усилена якорем и ключевой колонкой; APP_VERSION=0.2.41; Release-сборки успешны.
- Новый запрос: продолжать по порядку.
- Сделано: добавлены микро-пояснения терминов (XP в профиле, вес в каталоге навыков); APP_VERSION=0.2.42; Release-сборки успешны.
- Новый запрос: продолжать по порядку (этап 2.2).
- Сделано: добавлено пояснение термина "Источник" в логах; APP_VERSION=0.2.43; Release-сборки успешны.
- Сделано: добавлены пояснения "Архив" и "Прогрев" в админ-статистике; APP_VERSION=0.2.44; Release-сборки успешны.
- Сделано: добавлены пояснения про пресеты и фон в настройках интерфейса; APP_VERSION=0.2.45; Release-сборки успешны.
- Сделано: добавлено пояснение термина "Пайплайн"; APP_VERSION=0.2.46; Release-сборки успешны.
- Сделано: добавлены пояснения про кривую уровней и фокус-бонус в правилах; APP_VERSION=0.2.47; Release-сборки успешны.
- Сделано: добавлено пояснение параметров вращения/масштаба в 3D настройках; APP_VERSION=0.2.48; Release-сборки успешны.
- Сделано: проверены оставшиеся панели; начат этап 3.1 (смысловая сводка профиля); APP_VERSION=0.2.49; Release-сборки успешны.
- Сделано: добавлена карточка "Зоны перекоса" в профиле; APP_VERSION=0.2.50; Release-сборки успешны; изменения закоммичены.
- Сделано: мягкая подсветка перекоса в профиле (акцент на слабейшей категории + фон строки перекоса); APP_VERSION=0.2.51; Release-сборки успешны; изменения закоммичены.
- Сделано: мягкие акценты в диаграммах категорий, режимы профиля (обзор/аналитика), токены отступов и табы через декларацию; APP_VERSION=0.2.52; Release-сборки успешны; изменения закоммичены.
- Сделано: система заданий (мульти-профили, распределение вклада, tasks.json, вкладка задач в профиле и отдельная панель); APP_VERSION=0.2.54; Release-сборки успешны; изменения закоммичены.
- Сделано: фильтры/экспорт в "Задачах" (профиль/категория/период + CSV/TXT), подсказка обязательных полей в XP-модале; добавлен Помодоро (окно, настройки, подтверждение интервалов, звук, отсчёт в навигации); APP_VERSION=0.2.55; Release-сборки (CLI + GUI) успешны.
- Сделано: в навигации сокращён текст "Настройки интерфейса" → "Настройки UI" (влезает в кнопку); APP_VERSION=0.2.56; Release-сборки (CLI + GUI) успешны.
- Сделано: перегруппирована навигация (главное/админ), синхронизирован порядок меню/вкладок, Pomodoro получил герой‑блок; APP_VERSION=0.2.57; Release-сборки (CLI + GUI) успешны.
- Сделано: убран текст с узкого прогресс-бара ранга; добавлены аналоговые часы с цифровым дублем в Pomodoro; APP_VERSION=0.2.58; Release-сборки (CLI + GUI) успешны.
- Сделано: убран оверлей‑текст у прогресс-бара таймера Pomodoro; аналоговые часы перенесены в боковое меню с цифровым дублем; APP_VERSION=0.2.59; Release-сборки (CLI + GUI) успешны.
- Сделано: подогнан размер/выравнивание часов в боковом меню и усилен цифровой дубль; APP_VERSION=0.2.60; Release-сборки (CLI + GUI) успешны.
- Сделано: возвращён текст на прогресс-баре ранга; APP_VERSION=0.2.61; Release-сборки (CLI + GUI) успешны.
- Сделано: добавлена сводка задач (всего/последняя/фокус); APP_VERSION=0.2.62; Release-сборки (CLI + GUI) успешны.
- Сделано: добавлен мини-график активности задач за 14 дней; APP_VERSION=0.2.63; Release-сборки (CLI + GUI) успешны.
- Сделано: задачи стали кликабельны с карточкой деталей (опыт/навыки/участники), расширена сводка задач, исправлен тултип описания; UI-настройки доступны всем (обычным пользователям только полноэкранный/без рамки), звуки Помодоро доступны только администратору; APP_VERSION=0.2.66; Release-сборки (CLI + GUI) успешны; изменения закоммичены.
- Новый запрос: начать с окна "Ярлыки"; в режиме пользователя "Настройки интерфейса" моргают и не открываются.
- Сделано: окно "Ярлыки" + правая панель иконок, хранение `meta/shortcuts.json`, версия 0.2.67, сборки Release (CLI + GUI) успешны.
- Новый запрос: в правой панели "Ярлыки" вместо иконки показывать первые две гласные буквы названия программы.
- Сделано: вместо иконок в ярлыках показываются первые две гласные (fallback на первые две буквы), версия 0.2.68, сборки Release (CLI + GUI) успешны.
- Новый запрос: использовать первые две согласные (например Cascadeur -> "Cs") и добавить управление порядком ярлыков; моргания "Настроек интерфейса" нет.
- Сделано: бейджи ярлыков по двум согласным, порядок ярлыков настраивается (вверх/вниз), версия 0.2.69, сборки Release (CLI + GUI) успешны.
- Новый запрос: кнопка "Удалить" не влезает; правую плашку сделать уже; продолжить по этапу.
- Сделано: кнопка "Удалить" влезает (действия в колонке перестроены), правую плашку сузили, версия 0.2.70, сборки Release (CLI + GUI) успешны.
- Сделано: мягкий акцент на задачи слабой категории в "Задачах", версия 0.2.71, сборки Release (CLI + GUI) успешны.
- Сделано: мягкий акцент слабой категории в "Каталоге навыков", версия 0.2.72, сборки Release (CLI + GUI) успешны.
- Сделано: мягкий акцент слабой категории в админ-статистике (средние категории), версия 0.2.73, сборки Release (CLI + GUI) успешны.
- Сделано: мягкий акцент по источнику в логах (зона внимания), версия 0.2.74, сборки Release (CLI + GUI) успешны.
- Сделано: мягкий акцент фокусного этапа в "Пайплайне", версия 0.2.75, сборки Release (CLI + GUI) успешны.
- Сделано: мягкий акцент слабой категории в "Правилах" (базовый XP), версия 0.2.76, сборки Release (CLI + GUI) успешны.
- Новый запрос: продолжать этап 3.2.
- Новый запрос: пройтись по AgentsSkills и составить план стабильности/UX, предложения по кнопкам и реализация кнопки "Обзор" для выбора папки синхронизации.
- Сделано: в настройках синхронизации добавлена кнопка "Обзор..." (выбор папки через SHBrowseForFolderA).
- Сборки Release (CLI + GUI) успешны после добавления кнопки "Обзор...".
- Сделано: поле пути синхронизации перестроено в строку с кнопкой "Обзор...", заголовок вынесен отдельной строкой.
- Сборки Release (CLI + GUI) успешны после правки компоновки кнопки "Обзор...".
- Новый запрос: сделать выбор папки синхронизации внутри программы (без системного проводника).
- Сделано: добавлен мини-пикер папок внутри приложения (модал со списком подпапок, вверх/открыть/создать/выбрать).
- Сборки Release (CLI + GUI) успешны после добавления мини-пикера папок.
- Новый запрос: убрать создание папки, добавить выбор диска и поддержку кириллицы в путях.
- Сделано: в мини-пикере удалено создание папок, добавлен выбор диска; пути синхронизации читаются/пишутся в UTF-8 (u8path/u8string).
- Сборки Release (CLI + GUI) успешны после правок мини-пикера и UTF-8 путей.
- Сделано: убрана кнопка "Создать папку синхронизации" из настроек.
- Сборки Release (CLI + GUI) успешны после удаления кнопки.
- Новый запрос: добавить синхронизацию навыков и пайплайна.
- Сделано: синхронизация `skills.txt` и `meta/pipeline.json` добавлена в pull/push, очистка orphan в облаке; после pull перезагрузка каталога и пайплайна.
- Сборки Release (CLI + GUI) успешны после добавления синка навыков/пайплайна.
- Новый запрос: удалить все навыки по умолчанию.
- Сделано: список дефолтных навыков очищен (нет предустановленных).
- Сборки Release (CLI + GUI) успешны после удаления дефолтных навыков.
- Новый запрос: обнулить все навыки, чтобы не восстанавливались при синхронизации.
- Сделано: добавлена кнопка "Очистить все навыки" с подтверждением; очищает каталог и навыки в профилях.
- Сборки Release (CLI + GUI) успешны после добавления очистки навыков.
- Новый запрос: продолжать работу по плану.
- Сделано: начата стабильность - атомарная запись cloud.ini и manifest.ini.
- Сделано: атомарная запись skills.txt; в UI показ ошибки при неуспешном сохранении синхронизации.
- Сборки Release (CLI + GUI) успешны после правок сохранения навыков/синхронизации.
- Сделано: синхронизация теперь сообщает об I/O ошибках (pull/push) и помечает результат как ошибочный при сбое копирования.
- Сборки Release (CLI + GUI) успешны после добавления диагностики синка.
- Новый запрос: кириллица в навыках после синка/перезапуска отображается неверно (имя/вес).
- Сделано: загрузчик skills.txt теперь поддерживает BOM/UTF-16, чтобы корректно читать кириллицу.
- Сборки Release (CLI + GUI) успешны после правки загрузки навыков.
- Новый запрос: модал добавления навыка слишком широкий/текст не помещается; вес в каталоге корректен, но в редактировании сбрасывается на 1.0 и записывается в описание; проблема с неймингом (кириллица) остаётся.
- Сделано: модал добавления навыка ограничен по ширине и выровнены поля; парсер skills.txt поддерживает альтернативный порядок полей веса/описания; генерация ID переведена на classic locale, trim/weight парсинг теперь ASCII-стабильны; после pull перезагрузка каталога сбрасывает выбор для обновления редактора.
- Сборки Release (CLI + GUI) успешны после правок модала и парсинга навыков.
- Новый фидбек: в skills.txt строка отображается как mojibake `Р РµС„-Р±РѕСЂРґ` (пример `sk_...|Р РµС„-Р±РѕСЂРґ|1.2`) — подозрение на испорченную кодировку в файле.
- Сделано: добавлено авто-исправление mojibake (UTF-8, ошибочно прочитанного как CP1251) при загрузке навыков; исправленные значения пересохраняются в skills.txt.
- Сборки Release (CLI + GUI) успешны после добавления авто-исправления кодировки.
- Новый фидбек: в skills.txt виден mojibake с описанием (`sk_...|Р РµС„-Р±РѕСЂРґ|1.2|РћРїРёСЃР°РЅРёРµ...`), в UI имя правильное, но вес сбрасывается.
- Сделано: парсинг double для skills.txt и профилей переведён на ASCII-стабильный разбор (не зависит от локали с запятой), чтобы вес не сбрасывался после перезапуска/синка.
- Сборки Release (CLI + GUI) успешны после правки парсинга чисел.
- Новый запрос: отображать кириллицу в skills.txt корректно (Notepad) и требовать заполнения имени и описания при добавлении навыка.
- Сделано: skills.txt теперь сохраняется с BOM UTF-8; добавлена проверка обязательного описания в действиях добавления/редактирования навыка.
- Сборки Release (CLI + GUI) успешны после правок BOM/валидации описания.
- Новый запрос: добавить подсказку/предупреждение про обязательные поля при добавлении навыка.
- Сделано: в модале добавления навыка показано явное предупреждение, что имя и описание обязательны.
- Бета-этап (синхронизация): добавлен запрет на использование облачной папки, совпадающей или вложенной в локальное хранилище (Pull/Push возвращают предупреждение).
- Бета-этап (навыки): добавлено автоматическое исправление не только mojibake, но и строк, целиком сохранённых в CP1251 (конвертация в UTF-8 при загрузке навыков).
- Бета-этап (задачи): загрузка tasks.json теперь удаляет BOM UTF-8, чтобы корректно разбирать файлы с BOM.
- Бета-этап (план): продолжить проверки задач/XP (создание/сохранение/фильтры/экспорт/кириллица) и остальной UX/стабильность.
- Бета-этап (задачи): при неуспешной записи tasks.json показано предупреждение (XP начисляется, но уведомляем об ошибке сохранения файла).
- Бета-этап (задачи): при чтении tasks.json удаляем BOM UTF-8 (остается прежним), код комментария уточнён.
- Бета-этап (задачи): экспорт CSV/TXT теперь дописывает BOM UTF-8, чтобы Excel/блокноты корректно показывали кириллицу.
- Бета-этап (задачи): сохранение tasks.json теперь добавляет BOM UTF-8 для корректного отображения в Notepad и других редакторах.
- Бета-этап (задачи): при пустом результате фильтра добавлена кнопка быстрого сброса фильтров.
- Бета-этап (задачи): фильтрация вынесена в общую функцию, экспорты и таблица используют единый список (меньше расхождений).
- Бета-этап (задачи): счетчик показывает отфильтрованное количество и общий объём, сводка считает метрики по фильтру.
- Бета-этап (пайплайн): чтение pipeline.json удаляет BOM UTF-8; сохранение теперь пишет BOM для корректной кириллицы в редакторах.
- Бета-этап (ярлыки): чтение shortcuts.json удаляет BOM UTF-8; сохранение пишет BOM для корректной кириллицы в редакторах.
- Бета-этап (профили/ачивки): чтение профилей/ачивок из INI/JSON теперь убирает BOM UTF-8 (устойчивость к файлам из внешних редакторов).
- Бета-этап (облако): cloud.ini и manifest.ini читают BOM UTF-8, сохранение пишет BOM (правильная кириллица в редакторах).
- Бета-этап (UI настройки): ui.ini и пресеты читают BOM UTF-8, сохранение пишет BOM для корректной кириллицы.
- Бета-этап (правила): gameplay.ini читает BOM UTF-8, сохранение пишет BOM для корректной кириллицы.
- Бета-этап (админ): admin.ini читает BOM UTF-8, сохранение пишет BOM для корректной кириллицы.
- Бета-этап (отчеты): экспорт логов, профилей и админ-статистики пишет BOM UTF-8 (CSV/TXT) для корректной кириллицы в Excel/редакторах.
- Бета-этап (профили): seed-профили копируются только один раз (meta/seed.merged); удалённые профили больше не восстанавливаются при перезапуске.
- Бета-этап (ачивки): синхронизируются achievements/*.json и achievements/icons/*; пропущенные админ-профили не тянут свои ачивки; в облаке удаляются ачивки для отсутствующих профилей.
- Бета-этап (иконки ачивок): иконки из seed (`data/achievements/icons`) копируются в хранилище при старте, но только если файла ещё нет.
- Бета-этап (админ-пароль): пароль в admin.ini хранится с шифром Цезаря (сдвиг 7, префикс `caesar:`), при чтении поддерживаем старый открытый формат.
- Бета-этап (Помодоро): в левом меню добавлены быстрые кнопки (старт/сброс/следующий этап для ручного режима) с иконками.
- Бета-этап (ачивки файлы): achievements/*.json теперь читают BOM UTF-8 и сохраняются с BOM.

Done:
- Исправлена видимость списка профилей в главном меню.
- Внедрён техно-стекло-пак стилей без градиентов + синхрон плашки окна и фона.
- Смягчены пустые состояния/плейсхолдеры (TextDisabled), добавлены подсказки в правилах и логах.
- Отключено сохранение без изменений в правилах и настройках UI.
- В настройках UI добавлен индикатор "есть/нет несохранённых изменений".
- Главное меню переведено в верхнюю плашку; профили вынесены в отдельную вкладку; добавлен выбор профиля в окне профиля.
- В верхней плашке добавлены компактные icon-button'ы (профили/обновить/статистика/настройки/выход).
- В профиле: кольцевая диаграмма прогресса и сегменты категорий; KPI-карточки с иконками.
- Навигация: компактные кнопки с иконками (Home/Book/Flow/Cube/Lock/List и т.д.).
- Профиль → "Диаграммы": карточка категорий (sparkline + бары), карточка топ-навыков (бары), радар ограничен топ-N (регулируется слайдером).
- Профиль: диаграмма уровня центрирована в области.
- UX: окно "Профили" закрыто по умолчанию; последний профиль сохраняется/восстанавливается.
- Профиль: убран warning Dear ImGui (SetCursorPos) в заголовке.
- Каталог навыков: карточка "Сводка" (распределение категорий веса + mini-bars).
- Профиль: окно закреплено справа от навигации.
- Профиль → "Состояние": фиксированы ширины колонок, таймер не дрожит.
- Статистика: средние категории и ранги с mini-bars/sparkline.
- Layout: единая правая область с вкладками (Профиль/Профили/рабочие панели).
- Логи: сводка с KPI/иконками и спарклайном активности.
- Каталог навыков: сводка с KPI/иконками и спарклайном веса.
- Админ-статистика: KPI-карточки с иконками.
- Профиль: добавлена кнопка "Добавить опыт".
- Профиль: кнопка "Добавить опыт" перенесена под имя/ID.
- Название программы: ForgeMirror (UI/документация/окно/выходные файлы).
- Этап 1.2 (профиль): краткая сводка в верхней зоне, без дублей под диаграммой.
- Этап 1.3 (профиль): добавлены признаки загрузки и ошибки при выборе профиля; UI показывает состояние загрузки, ошибку и пустые списки; изменения закоммичены; APP_VERSION=0.2.13.
- Этап 1.1 (каталог навыков): подсказки действий, пустой стейт каталога, CTA для добавления при пустом выборе.
- Этап 1.1 (пайплайн): подсказки действий и empty-state фильтра.
- Этап 1.1 (логи): подсказки действий, empty-state и сброс фильтров.
- Этап 1.1 (статистика): подсказки назначения и empty-state с быстрым сбросом фильтров.
- Этап 1.1 (правила): подсказки назначения и контекста влияния на прогресс.
- Этап 1.1 (3D просмотр): подсказки управления и контекст ошибки.
- Этап 1.1 (3D настройки): подсказки назначения, empty-state и сброс фильтра.
- Этап 1.1 (настройки интерфейса): подсказки назначения и empty-state фонов.
- Этап 1.1 (главное меню): подсказки назначения, быстрые действия и empty-state профилей.
- Этап 1.1 (навигация): подсказка назначения и статус админ-разделов.
- UI: сворачиваемые секции по умолчанию свернуты.
- Этап 2.1: таблица "Навыки" профиля усилена (ключевой столбец, микро-якоря, ритм топ-строк).
- Этап 2.1: таблица "Логи" усилена (ключевой столбец, микро-якоря, акцент важного).
- Этап 2.1: таблица "Каталог навыков" усилена (ключевой столбец, микро-якоря, ритм топ-строк).
- Этап 2.1: таблицы "Админ-статистика" усилены (ключевой столбец, микро-якоря, ритм топ-строк).
- Этап 2.1: таблица "Топ навыков" профиля усилена (ключевой столбец, микро-якоря, ритм топ-строк).
- Этап 2.1: список "Пайплайн" усилен (ключевой столбец, микро-якоря).
- Этап 2.1: таблица "Состояние" профиля усилена (ключевой столбец, микро-якоря, акцент проблемных строк).
- Этап 2.1: таблицы "Правила" усилены (ключевой столбец, микро-якоря, единицы).
- Этап 2.1: табличные фильтры "3D/Настройки UI" усилены (микро-якоря, ключевой столбец).
- Этап 2.1: таблица "Индикаторы" профиля усилена (ключевой столбец, микро-якоря, акцент проблемных строк).
- Этап 2.1: табличные блоки действий в модальных окнах усилены (микро-якоря, ключевой столбец).
- Этап 2.1: прочие табличные фильтры/экшены усилены (микро-якоря, ключевой столбец).
- UI: исправлена компоновка кнопок в "Пользовательских пресетах".
- Якоря: числовые микро-якоря заменены на иконки.
- XP-модал: таблица распределения навыков усилена (якоря, ключевой столбец, ритм топ-строк).
- Профиль: фильтры ачивок/навыков/активности усилены (икон-якоря, ключевой столбец).
- Логи: фильтр-строка усилена (икон-якорь, ключевая колонка).
- Админ: строки обновления/фильтров усилены (икон-якорь, ключевая колонка).
- Терминология: добавлены короткие пояснения XP (профиль) и веса (каталог навыков).
- Терминология: добавлено пояснение источника в логах.
- Терминология: добавлены пояснения архив/прогрев в админ-статистике.
- Терминология: добавлены пояснения пресетов и фона в настройках интерфейса.
- Терминология: добавлено пояснение пайплайна.
- Терминология: добавлены пояснения кривой уровней и фокуса в правилах.
- Терминология: добавлено пояснение параметров Yaw/Pitch/Zoom в 3D настройках.
- Этап 3.1: добавлена карточка "Сводка состояния" в профиле (состояние/перекос/фокус).
- Этап 3.1: добавлена карточка "Зоны перекоса" (топ-3 слабые категории).
- Этап 3.2: мягкий акцент слабейшей категории в профиле (фон/цвет).
- Этап 3.2: мягкий акцент слабейшей категории в диаграммах категорий.
- Этап 4.1: режимы интерфейса в профиле (обзор/аналитика).
- Этап 5.1: зафиксированы базовые токены отступов/карточек.
- Этап 5.2: табы профиля вынесены в декларацию для расширения.
- Система заданий: расширен XP-модал (проект/задача/описание, выбор профилей и вклад), хранение в `data/meta/tasks.json`, вкладка "Задачи" с последними 5, отдельная панель всех задач.
- Окно "Ярлыки": добавление/удаление/запуск, правая панель иконок, данные в `meta/shortcuts.json`.
- Ярлыки: бейдж из двух гласных вместо пиктограммы.
- Ярлыки: бейдж из двух согласных, добавлены кнопки порядка.
- Ярлыки: скорректирована компоновка кнопок действий и ширина правой плашки.
- Этап 3.2: мягкий акцент проблемной категории в таблице задач.
- Этап 3.2: мягкий акцент слабой категории в "Каталоге навыков".
- Этап 3.2: мягкий акцент слабой категории в "Админ-статистике".
- Этап 3.2: мягкий акцент источника в "Логах".
- Этап 3.2: мягкий акцент фокусного этапа в "Пайплайне".
- Этап 3.2: мягкий акцент слабой категории в "Правилах".

Now:
- Разобрать, почему папка синхронизации пустая (роль пользователя/выгрузка/настройки).
- Собрать установщик и проверить запуск на другом ПК.
- Подготовить подробную инструкцию по запуску установщика и обновлению через облако.
- Разобрать различия профилей/темы между сборкой и установщиком; подготовить иконку.
- Проверить индикатор синхронизации после сборки GUI.
- Проверить отсутствие дублирующего Admin-профиля после изменений.
- Проверить удаление лишних профилей в облаке при push.
- Составить план стабильности/UX и предложить правки по кнопкам, включая кнопку "Обзор" в синхронизации.

Next:
- Подтвердить сценарий выгрузки администратором и при необходимости уточнить/усилить UI-статусы синхронизации.
- Согласовать вариант установки (per-user/per-machine) и схему обновления через облако.
- Утвердить список UX-изменений (добавить/убрать кнопки) и реализацию выбора папки синхронизации.
- После фиксов проверить: создание навыка (кириллица), синк/перезапуск, корректный вес в каталоге и в редактировании.

Open questions (UNCONFIRMED if needed):
- Пользователь в режиме просмотра или админ? Выполнялась ли выгрузка администратором в облако?
- Выбран режим установки per-user (без UAC).
- Иконка создана (минимальная FM); при необходимости заменить.

Working set (files/ids/commands):
- `AgentsSkills/CONTINUITY.md`
- `AgentsSkills/AGENTS.md`
- `gui/GuiTasksPanel.inc`
- `gui/GuiState.inc`
- `gui/GuiProfileSections.inc`
- `gui/GuiPomodoroPanel.inc`
- `gui/GuiUiSettingsPanel.inc`
- `gui/GuiMainMenuPanel.inc`
- `gui/GuiNavigationPanel.inc`
- `gui/GuiWorkspacePanel.inc`
- `CMakeLists.txt`
- `installer/ForgeMirror.iss`
- `installer/build-installer.ps1`
- `gui/GuiMainMenuPanel.inc`
- `gui/GuiState.inc`
- `gui/GuiApp.cpp`
- `gui/GuiStorageUtils.inc`
- `main.cpp`
- `cmake --build build --config Release`
- `cmake --build build-gui --config Release --target JobSkillGui`
- `cmake --build build --config Release`
- `cmake --build build-gui --config Release --target JobSkillGui`





