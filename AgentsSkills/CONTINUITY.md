UI аудит:
- Разный размер кнопок: где-то full width, где-то SmallButton/иконка без единой иерархии.
- Карточки/секции используют разные отступы (CardPadding/WindowPadding/ItemSpacing не синхронизированы).
- Таблицы: местами BordersInnerH/V включены, местами нет — визуальный шум.
- Заголовки секций и подписи разного веса/контраста (TextDisabled не задан в 0/1/2).
- Ховеры списков/строк не единообразны (RowBg vs Header/Frame).
- Навигация: активные состояния иконок/текста не всегда синхронизированы.

# CONTINUITY

- Goal (incl. success criteria):
  - Стабильная Beta: корректный UI/UX, синхронизация, сборка/инсталлер, версии + patch-notes на каждую версию.
  - Система добавления опыта без «ездящих» ползунков: оценка навыков 0–5 и авто‑доли, проценты корректно отображаются.
- Constraints/Assumptions:
  - Следовать AgentsSkills UI‑гайдам: `AGENT_UI_TOKENS.md`, `AGENT_UI_GRID.md`, `ForgeMirror_DesignCode.md`, `PR_UI_CHECKLIST.md`.
  - Версионирование: major.any.minor (minor 2‑значный).
- Key decisions:
  - Единая переменная APP_VERSION в CMake для версии приложения.
  - Откат UI/UX изменений последних двух дней (возврат стабильного UI).
  - Добавление опыта: оценки 0–5 (ползунки), доли рассчитываются автоматически.
  - Синхронизация: whitelist профилей/skills/tasks/pipeline/gameplay/achievements/icons/meta/patch-notes, очистка лишнего (админ).
- State:
  - Ветка: taskmanager. Версия: 0.3.97.
  - Последнее изменение: профильные операции вынесены в AppProfileService, GUI теперь не грузит и не выбирает профиль напрямую из storage.
- Done:
  - Архитектура GUI: профильные операции вынесены в `AppProfileService` (`LoadSortedProfiles` / `LoadActiveProfile` / `ReloadProfiles`), `GuiProfileOps` переведён на service-слой.
  - Архитектура GUI: вынесены доменные типы в `include/AppDomainTypes.h`, добавлен `AppContext`, инициализация/перезагрузка synced-данных сведена в общий helper-слой.
  - Задачи: исправлены подписи таблицы "Назначить профилям" (читаемые заголовки).
  - Профили: локальная доверенность устройства (30/90 дней, ui.ini, без синхронизации) + отображение срока/сессии.
  - Проекты: удаление проекта переведено на безопасный сценарий (проверка сохранения `projects/tasks`, откат в памяти и на диске при ошибке).
  - Проекты: подсветка строк с просроченными задачами + подсказка по сокращениям статусов, сброс фильтра очищает переключатель.
  - Проекты: добавлены фильтры по названию/описанию и переключатель `Только просроченные`, с индикацией количества отфильтрованных проектов.
  - Проекты: защита от дубликатов имён (case-insensitive) с понятной ошибкой.
  - Проекты: добавлена сортировка (название / задач / просроченных).
  - Проекты: фильтр/переключатель просрочки/сортировка сохраняются в ui.ini.
  - Проекты: аудит отвязки задач при удалении проекта (`meta/task-audit.log`).
  - Задачи: быстрый фильтр «Без проекта» в верхней полосе.
  - Проекты: добавлена сводка по задачам в строке проекта (`Н/В/Г` + просроченные) и кнопка `К задачам` с авто-фильтром по проекту.
  - Задачи: в `Массовые действия` добавлено массовое изменение приоритета для выбранных задач (с сохранением, откатом при ошибке и аудитом `priority`).
  - Задачи: добавлен фильтр по проекту (`Все`/`Без проекта`/конкретный проект) в оба режима фильтров.
  - Логи: аудит задач получил фильтры (актор/задача/поле) и кнопку открытия `meta/task-audit.log`.
  - Логи: добавлен блок `Аудит задач` + экспорт task-audit в `meta/reports`.
  - Задачи: добавлен журнал изменений `meta/task-audit.log` (create/status/priority/project/deadline/assignees/delete + bulk-операции).
  - Задачи: в панели задач добавлен блок `Последние изменения` (время, актор, задача, поле, изменение).
  - Синхронизация: `meta/task-audit.log` включен в cloud push/pull/whitelist-cleanup и health-check whitelist.
  - Задачи: добавлен блок "Скоро дедлайн" + авто-напоминания 24ч/2ч с анти-спам защитой.
  - Задачи: добавлен приоритет (модель+UI+фильтр+сортировка+inline-редактирование).
  - Задачи: блок массовых действий уплотнён (компактный layout + popup для исполнителей).
  - Задачи: безопасные переходы статусов + атомарные массовые операции (одно сохранение/откат при ошибке).
  - Задачи: быстрые фильтры + inline-редактирование (дедлайн/проект/исполнители/статус) + массовые действия по выбранным задачам.
  - Задачи: добавлено быстрое изменение статуса в таблице (админ), с единым сохранением и откатом при ошибке.
  - Модуль задач расширен: админское создание задач (название, проект, описание, дедлайн дата+время, статус) и назначение на несколько профилей.
  - Добавлен новый админский модуль `Проекты` (`meta/projects.json`) с CRUD и привязкой задач по `projectId`.
  - Синхронизация обновлена: `meta/projects.json` добавлен в cloud push/pull/whitelist-cleanup.
  - QA: smoke_core расширен кейсом для `meta/storage.json` с поврежденными разделителями в числах (`updated_at`, `pomodoro_end`) + проверка нормализации после сохранения.
  - Хранилище: чтение числовых полей storage.json устойчиво к повреждённым символам.

  - Облако: при pull кошельки профилей сохраняются по максимуму (монеты не теряются).

  - Помодоро: начисление монеты учитывает фактическую длительность (>= минимум).

  - Облако: wallet_enc теперь перезаписывается при сохранении кошелька (не теряется при pull).

  - Хранилище: добавлена ревизия storage.json, синхронизация настроек помодоро идёт по rev.


  - Хранилище: storage.json получает updated_at; при sync интервалы помодоро не сбрасываются.

  - Обновления: кнопка уведомления теперь называется "Установить".

  - Обновления: уведомление при старте, если в releases есть новая версия (Скачать/Закрыть).
  - Облако: обычные пользователи при sync отправляют в облако только свой кошелёк.

  - Облако: обычные пользователи при sync отправляют в облако только свой кошелёк.

  - Профили: при pull кошелёк сохраняется, если локальные данные новее/полнее облачных.
  - Помодоро: окно начисления проверяется по времени старта фокуса.

  - Облако: storage.json копируется из облака только если файл новее локального.
  - Помодоро: окно начисления проверяется по времени старта фокус-сессии.

  - Облако: при quick sync (push+pull) админские данные отправляются до pull, настройки хранилища сохраняются.

  - Синхронизация: ярлыки (meta/shortcuts.json) исключены из облака и установщика (локальные настройки).
  - Синхронизация: при pull не сбрасывается пароль профиля, если в облаке нет пароля.
  - Помодоро: правила начисления монет синхронизируются через meta/storage.json.

  - Профили: логин/пароль (шифрование, синк), автогенерация при создании + модал с данными доступа.
  - Помодоро: индикатор доступности начисления монет (расписание/вход/минимум).
  - Монеты: баланс хранилища и кошельков сохраняется в зашифрованном виде (balance_enc/wallet_enc).
  - Помодоро: начисления не пишутся в журнал хранилища.
  - Облако: папка releases в синхронизации разрешена и не удаляется при очистке.
  - Профиль: исправлены несоответствия BeginCard/EndCard (ошибки ImGui стека).
  - Профили: ввод пароля один раз на ПК (статус доступа), смена пароля, админ‑сброс с генерацией нового.
  - Профили: блокировка/разблокировка с подтверждением (админ), статус в профиле и списке.
  - Профили: аудит действий (meta/profile-audit.log) + синхронизация.
  - Профили: сохранение unlocked/recent в ui.ini, список «Недавние», поиск по логину + фильтр по профессии.
  - Сборка GUI: ok (Release).

  - Баннер: расширена колонка "Редактировать" в списке фраз, чтобы текст не обрезался.
  - Сборка GUI: ok (Release).

  - Баннер: список фраз переведён на таблицу, кнопки удаления/редактирования больше не выходят за границы.
  - Сборка GUI: ok (Release).

  - Баннер: текст центрируется, если помещается; добавлено редактирование фраз (сохранить/отмена).
  - Сборка GUI: ok (Release).

  - Пароли: подсказки раскладки/Caps/Shift в модалках, индикатор в верхней панели.
  - Сборка GUI: ok (Release).

  - Баннер: добавлена панель вывода под верхним меню (полная ширина), пустой блок при отсутствии фраз.
  - Баннер: админ-модуль для добавления/удаления фраз, хранение в meta/banner.json, синхронизация через облако.
  - Баннер: авто-ротация раз в минуту и бегущая строка при переполнении.
  - Сборка GUI: ok (Release).
- UI: унифицированы иконки/икон-кнопки (убран glow, рамка только при hover/selected).
- UI: ослаблен контраст рамок карточек/бейджей.

  - Хранилище: копирование из legacy в основной каталог, seed-профили отключены.
  - Хранилище: при наличии пользовательской папки используем её, seed копируется туда.
  - Модульность: guards для задач/помодоро/профессий/ярлыков/пайплайна и вкладок профиля.
  - Модульность: добавлены guards для ачивок в UI.
  - Инсталлер: починен парсинг APP_VERSION из CMakeLists.txt.
  - Профиль: добавлено отображение профессии в строке KPI.
  - Добавление опыта: список навыков фильтруется по профессии (и общим).
  - Навыки: добавлены подсказки про общие навыки и фильтр профессий.
  - Навыки: исправлен блок таблицы/группировки (ошибка ImGui EndTable).
  - Навыки: категории убраны из добавления/редактирования, можно привязать навык к нескольким профессиям.
  - Навыки: фильтрация по профессии учитывает множественные привязки.
  - Сборка GUI: ok (Release).
  - Навыки: в сводке оставлен бар по категориям веса (A-E), спарклайн убран.
  - Навыки: действия (добавить/удалить/очистить) перенесены под сводку.
  - Восстановлены AgentsSkills UI‑гайды.
  - Реализованы оценки 0–5 для навыков и авто‑доли в модале XP.
  - Добавлены patch‑notes до 0.3.18, сборка GUI проходит.
  - Удалены legacy-упоминания старого названия (код/доки), оставлен ForgeMirror.
  - Профессии/навыки: при смене профессии сбрасывается выбор и пересчёт долей XP.
  - Синхронизация: добавлен shortcuts.json, очистка облака по whitelist, расширенный отчёт.
  - Бета-готовность: актуализирован installer fallback, обновлены доки обновлений.
  - UX: в профиле путь отчётов с копированием, в задачах корректная подготовка формы XP.
  - UX: копирование пути отчётов в одну строку при достаточной ширине.
  - Профессии: исправлено удаление (уникальные ID в строке).
  - Профессии: кнопка удаления "?" и разрешён клик поверх строки.
  - Профессии: подтверждение удаления, кнопка "Х".
  - Профессии: кликабельная кнопка удаления (без SpanAllColumns).
  - Профессии: окно подтверждения нормальной ширины, текст в 2 строки.
  - Health-check: индикатор наличия проектного data/ (админ, меню облака).
  - GUI: консоль отключена всегда (Win32 subsystem + WinMain wrapper).
- Remaining (taskmanager):
  - (пусто)



